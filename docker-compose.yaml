services:

  tgbot:
    image: ghcr.io/skoval67/easybot:${image_tag}
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      RAPID_API_KEY: ${RAPID_API_KEY}
    volumes:
      - bothistory/history.db:/app/history.db

volumes:
  bothistory:
