services:

  tgbot:
    image: ghcr.io/skoval67/easybot:${image_tag}
    environment:
      BOT_TOKEN: ${BOT_TOKEN}
      RAPID_API_KEY: ${RAPID_API_KEY}
    volumes:
      - botlog:/app/mybot.log
      - bothistory:/app/history.db

volumes:
  botlog:
  bothistory:
